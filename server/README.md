# Tony's Workbench Server

This folder is reserved for the Node.js Backend-for-Frontend module.

The BFF should expose same-origin `/api/...` routes for the Angular client, validate browser requests, shape browser-facing responses, map safe errors, and call Java services in `tonys-workbench-services`.

Do not add Express or BFF runtime dependencies until an actual server module is implemented.
